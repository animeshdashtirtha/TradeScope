import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors } from '../theme/colors';
import { InsiderTrade } from '../types/trade';
import { formatCurrency, formatDate } from '../utils/formatters';
import { SignalBadge } from './SignalBadge';
export function TradeCard({ trade, onPress }: { trade: InsiderTrade; onPress: () => void }) { const isPurchase = trade.type === 'Purchase'; return <Pressable accessibilityRole="button" accessibilityLabel={`${trade.company}, ${trade.type.toLowerCase()}, ${formatCurrency(trade.value, true)}. Open details`} onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={styles.top}><View><Text style={styles.ticker}>{trade.ticker}</Text><Text style={styles.company}>{trade.company}</Text></View><SignalBadge signal={trade.signal} /></View><View style={styles.bottom}><View><Text style={[styles.type, { color: isPurchase ? colors.purchase : colors.sale }]}>{isPurchase ? '↗ PURCHASE' : '↘ SALE'}</Text><Text style={styles.meta}>{trade.insider} · {trade.role} · {formatDate(trade.transactionDate)}</Text></View><Text style={styles.value}>{formatCurrency(trade.value, true)}</Text></View></Pressable>; }
const styles = StyleSheet.create({ card: { backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, borderRadius: 14, padding: 16, marginBottom: 10 }, pressed: { opacity: 0.75 }, top: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start' }, ticker: { color: colors.text, fontSize: 17, fontWeight: '800', letterSpacing: 0.3 }, company: { color: colors.muted, fontSize: 12, marginTop: 3 }, bottom: { marginTop: 18, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-end' }, type: { fontSize: 11, fontWeight: '800', letterSpacing: 0.7 }, meta: { color: colors.subtle, fontSize: 11, marginTop: 5 }, value: { color: colors.text, fontSize: 18, fontWeight: '800' } });