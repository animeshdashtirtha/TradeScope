import { StyleSheet, Text, View } from 'react-native';
import { colors } from '../theme/colors';
export function MockActivityChart() { const bars = [35, 58, 42, 76, 54, 88, 68]; return <View style={styles.wrap}><View style={styles.chart}>{bars.map((height, index) => <View key={index} style={styles.column}><View style={[styles.bar, { height: `${height}%`, backgroundColor: index === 5 ? colors.accent : `${colors.accent}70` }]} /><Text style={styles.day}>{['M', 'T', 'W', 'T', 'F', 'S', 'S'][index]}</Text></View>)}</View></View>; }
const styles = StyleSheet.create({ wrap: { backgroundColor: colors.surface, borderRadius: 14, borderWidth: 1, borderColor: colors.border, padding: 16 }, chart: { height: 120, flexDirection: 'row', alignItems: 'flex-end', justifyContent: 'space-around' }, column: { height: '100%', alignItems: 'center', justifyContent: 'flex-end', width: 24 }, bar: { width: 10, borderRadius: 5, minHeight: 8 }, day: { color: colors.subtle, fontSize: 10, marginTop: 8 } });